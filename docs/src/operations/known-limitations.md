# 7. Known Limitations

## Replication security — **deploy only on a trusted network**

The May-2026 security review (see
the 2026 review) identified
six blocker-class security gaps in the replication wire
protocol. Until those are closed, **the replication
subsystem must not be deployed across an untrusted network
boundary**:

- The replication wire protocol has no authentication. A peer
  identity (`group_name`, `node_name`) is self-claimed
  plaintext and not verified.
- **`peer_allowlist` (mTLS Phase 2 — v3.1.0)**: `RepConfig::peer_allowlist`
  is now enforced at the TLS handshake layer via
  `TlsTcpChannelListener::bind_with_tls_and_allowlist`.  Peers whose
  certificate Subject CN or DNS SAN does not match the configured list
  are rejected before any application data is exchanged.  Requires
  `RepTransportKind::Tls`; see the replication security setup guide.
- `NetworkRestoreServer` streams the entire on-disk
  environment to anyone who connects to its port.
- `PeerFeederService` streams the WAL to anyone who connects.
- Election proposals and votes are unsigned and unauthenticated.
  An on-path attacker can flip the cluster master.
- `NetworkRestore` (client) trusts server-supplied filenames —
  a malicious peer can write attacker-controlled bytes to any
  filesystem path the noxu process can write
  (path traversal).
- TCP frame `payload_len` (32-bit) is unbounded — a single
  attacker frame can trigger a 4 GiB allocation.
- The QUIC channel's ergonomic constructor (`QuicChannel::connect`)
  installs a no-op `ServerCertVerifier`. The user must
  explicitly opt OUT of skip-verification by using
  `connect_with_config` to get authenticated TLS.

Recommended deployment until these are remediated:

- Replicate only across a host-firewalled or
  VPC-segmented network where every peer IP is statically
  known and the firewall blocks all other inbound traffic.
- Do not expose the replication port on any
  internet-reachable interface.
- Treat the replication network as trusted: if a peer is
  compromised, the entire replication group is
  compromised.

## Other limitations

| Limitation | Status | Workaround |
|-----------|--------|------------|
| **Concurrent write throughput gap** | Known — Noxu LockManager uses 64 shards; alternative implementations using per-slot lock designs scale better at 16+ concurrent writers | Keep writer concurrency ≤ 8 threads per environment for optimal throughput; use disjoint key ranges when possible |
| **TiB-scale validation not automated** | `examples/scale_validation.rs` is a manual pre-production check; not run in CI | Run manually: `cargo run --example scale_validation -- --records 10000000 --threads 8` |
| **Sustained slow-test suite not in default CI** | P4/P5 tests marked `#[ignore]` to avoid CI timeouts | Run explicitly: `cargo nextest run -p noxu-db --profile slow --run-ignored all` |
| **`TupleSerdeBinding` value encoding is serde (not order-preserving)** | `TupleSerdeBinding` encodes KEYS with the sort-preserving tuple encoder (range scans on tuple keys work correctly) and encodes the DATA/value with serde binary. Only the value encoding is non-order-preserving — which is correct, since the B-tree compares keys, not values. (The earlier wording wrongly implied the keys were not sortable.) | None for keys — tuple-keyed range scans are correct. Do not rely on value-byte ordering (values are not a sort dimension). |
| **Property-based tests timeout in fast nextest runs** | `noxu-db::prop_tests` and `noxu-collections::prop_tests` may timeout under default 60 s limit | Run with `--profile slow` or increase timeout in `.config/nextest.toml` |
| **Replication: server-side network restore** | TCP file transfer implemented; client-side `NetworkRestore::execute()` complete | Full production hardening of restore protocol is recommended before use in HA deployments |
| **`ReplicatedEnvironment::new` does not start the replication group** | Identified by May-2026 claim audit. The doc claims `new()` initiates an election and contacts peers; the body only constructs state and starts the TCP service dispatcher. | The replication subsystem requires further wiring (election trigger, peer contact) before it provides the documented behaviour. |
| **`become_master` active feeder threads + WAL-scanner auto-feed** | **Implemented** (v3.2.0 push-feeder threads, v4.0.0 WAL-scanner auto-feed, C-C2+C-C2b). `become_master` spawns `FeederRunner` threads per registered channel. When `with_environment(env_impl)` has been called the `FeederRunner` uses `EnvironmentLogScanner` to auto-discover VLSN-tagged WAL entries written by real `EnvironmentImpl` commits — no `replicate_entry` call needed. Without a wired env the fallback in-memory queue (populated by `replicate_entry`) still works. Convergence tested end-to-end with real `EnvironmentImpl` commits. | Call `with_environment(env_impl)` then `register_feeder_channel` then `become_master`. The WAL-scanner auto-feed path is active; `replicate_entry` is still supported for backwards compatibility and for callers that do not use `EnvironmentImpl`. |
| **`transfer_master`** | Substantially implemented (v2.0+): sends network messages and demotes self to replica. Some edge cases (election quorum enforcement during transfer) are not covered. | Use `transfer_master` for graceful topology changes; monitor via `StateChangeListener` for the role transition. |
| **`shutdown_group` catch-up wait** | **Implemented** (v3.2.0, M-4): when `FeederRunner` threads are active, `shutdown_group` waits up to half the timeout for each replica to ack the master's VLSN before sending `SHUTDOWN_GROUP`. Pull-path replicas (no registered channel) receive `SHUTDOWN_GROUP` without a VLSN-level wait. | Use `shutdown_group` with a generous timeout (≥ 10 s) to give replicas time to catch up. |
| **Built-in metrics export** | **Implemented** (Unreleased). `env.get_stats()` is now continuously exportable to the [`metrics`](https://docs.rs/metrics) facade via `noxu_db::metrics_export::MetricsExporter` (periodic sampler) and `noxu_observe::export` (the JE-StatGroup→facade mapping), behind the `observability` feature. Any `metrics` recorder collects them; an optional `prometheus` feature adds a `/metrics` exposition convenience. Default-off and zero-cost when disabled. | Enable `observability` on `noxu-db`, install a recorder, start `MetricsExporter`. See `docs/src/operations/monitoring.md`. Note: `noxu_db_pri_*` throughput metrics read 0 until the engine wires its per-DB throughput counters. |
| **Stats-file dump (`STATS_FILE_*`) (implemented, 7.1)** | When `stats_collect` is enabled, a `noxu-stats-file` daemon (faithful analogue of JE `StatCapture`, gated by `STATS_COLLECT`) samples the same snapshot `Environment::stats()` returns and appends a CSV row to a rotating stats file (`noxu.stat.<N>.csv`) in `stats_file_directory` (default: env home) every `stats_collect_interval_secs`. After `stats_file_row_count` rows it rotates; at most `stats_max_files` files are retained (oldest pruned). Self-contained CSV — no external recorder needed. JE ref: `EnvironmentParams.STATS_FILE_DIRECTORY` / `STATS_FILE_ROW_COUNT` / `STATS_MAX_FILES`, `com.sleepycat.je.statcap.StatCapture`. | Set `with_stats_collect(true)` (+ optionally `with_stats_file_directory`, `with_stats_file_row_count`, `with_stats_max_files`). For a live metrics pipeline use `metrics_export` (the `observability` feature) instead. |
| **`startup_dump_threshold_ms` startup summary (implemented, 7.1)** | If `Environment::open` takes at least the configured threshold, Noxu logs a `warn!` startup summary with the elapsed open time (dominated by crash recovery) plus a `get_stats()` snapshot. Threshold `0` (default) disables it. JE ref: `EnvironmentParams.STARTUP_DUMP_THRESHOLD`. | Set `with_startup_dump_threshold_ms(ms)` to surface slow-recovery startups in the log. |
| **`Environment::get_lock_stats()` / `get_transaction_stats()`** | JE exposes separate lock-table and transaction-subsystem stat surfaces. Noxu has only `get_stats()` (Wave 11-R audit finding 3-C). | Use `get_stats()` for aggregate numbers; per-lock-table granularity is not available. |
| **`LogFlushTask` — no public type** | Background log-flush daemon (`LOG_FLUSH_NO_SYNC_INTERVAL`) runs internally but is not exposed as a public API (Wave 11-R audit finding 3-F). | Set `with_log_group_commit` + `with_durability(CommitNoSync)` and rely on the daemon; no manual flush handle is available. |
| **`env_check_leaks` (implemented, 7.1)** | At `Environment::close`, when `env_check_leaks` is `true` (the default), Noxu walks the active lock table (`LockManager::report_leaked_locks`) and logs a `warn!` for any lock still held with an owner — an application leak (a dropped `Transaction` or a cursor held open). Diagnostic only: it reports the leaked `(lsn, owner_locker_ids)`, it does not force-release or fail the close. JE ref: `EnvironmentImpl` leak checking. | Watch the log at close for `env_check_leaks:` warnings; set `env_check_leaks(false)` to silence the walk. |
| **`exception_listener` (JE `ExceptionListener`) (implemented, 7.1)** | Register a callback on `EnvironmentConfig::with_exception_listener(Arc<dyn ExceptionListener>)`. When a background daemon (checkpointer / cleaner / log-flusher) encounters a recoverable error — previously swallowed — the listener's `exception_event(&ExceptionEvent)` fires with the daemon `source`, the error `message`, and the OS thread name. The callback runs on the daemon thread (keep it cheap, non-panicking). Wired via a `noxu_config::ExceptionDispatcher` shared into each daemon at spawn and installed by `Environment::open` before any daemon does work. JE ref: `com.sleepycat.je.ExceptionListener`, `EnvironmentImpl` daemon catch blocks. | Register a listener to observe async daemon failures you otherwise could not see. Without one, dispatch is a no-op (zero cost). |
| **`env_forced_yield` (implemented, 7.1)** | Wired into `noxu-latch`. When `true`, the latch layer injects `std::thread::yield_now()` at each latch acquire (post-grant) and release point — a test-only fairness-stress knob that helps shake out latch-ordering races (faithful analogue of JE `ENV_FORCED_YIELD`). When `false` (the default) the injection point is a single relaxed atomic load, so it is effectively free — production behaviour is unchanged. Installed process-globally at `Environment::open` via `noxu_latch::configure`. Removed from the `unimplemented_params` WARN registry. JE ref: `EnvironmentParams.ENV_FORCED_YIELD`. | Set `with_env_forced_yield(true)` in tests to stress latch ordering; leave `false` (default) in production. |
| **`env_fair_latches` (reserved — deferred, needs a latch rewrite)** | FIFO-ordered (no-barging) latch acquisition (JE `setFairLatches` / `ENV_FAIR_LATCHES`). **Not implemented and deliberately not faked.** Noxu's latches (`noxu-latch`) are backed by `noxu-sync`'s futex-based `Mutex`/`RwLock`, which are fundamentally **non-fair** — a new arrival can barge ahead of a queued waiter — and expose no FIFO wait queue to toggle. A faithful fair-latch mode is a dedicated latch rewrite (a ticket/FIFO wait queue in `noxu-sync`, or a fair-mode wrapper), not a config flag flip, so it is left honestly reserved rather than shipped as a fake flag. Setting `true` emits a `WARN` and has no effect. JE ref: `EnvironmentConfig.setFairLatches`, `EnvironmentParams.ENV_FAIR_LATCHES`. | Has no effect; latches do not guarantee FIFO order. A fair-latch mode is tracked as a dedicated `noxu-sync` FIFO effort. |
| **`env_latch_timeout_ms` (implemented, 7.1)** | Wired into `noxu-latch`'s exclusive and shared acquire paths. A non-zero timeout turns a latch deadlock — which would otherwise hang — into a diagnosable `NoxuError::LatchTimeout` error: if a latch cannot be acquired within the timeout, `acquire` returns `LatchError::Timeout` (naming the latch) instead of blocking. `0` means no timeout (block until acquired). The default `300_000` (5 min) is treated as the "unset" sentinel — leaving it unchanged preserves the historical latch behaviour (a 5 s acquire timeout) byte-for-byte; setting any other value opts in. Installed process-globally at `Environment::open` via `noxu_latch::configure`. Removed from the `unimplemented_params` WARN registry. JE ref: `EnvironmentParams.ENV_LATCH_TIMEOUT`. | Set `with_env_latch_timeout_ms(ms)` to fail (rather than hang) on a latch that cannot be acquired within `ms`; `0` disables the timeout. |
| **`env_ttl_clock_tolerance_ms` (reserved, v3.1)** | Stored but never read; TTL expiration is not implemented. Setting to non-zero emits a `WARN` log. | Has no effect; records are never expired by the engine. |
| **`env_expiration_enabled` (reserved, v3.1)** | Stored but never read; TTL-based record expiration is not implemented. Setting to `true` emits a `WARN` log. | Has no effect; records are never expired by the engine. |
| **Per-record TTL is hours-granularity only** | `WriteOptions::with_ttl(hours)` and `with_expiration(hours)` store expiration as hours since the Unix epoch. Seconds-granularity TTL is **not supported**: `ttl_secs_to_expiration` exists as a utility function but the engine's read path (`is_expired`) consults `expiration_in_hours = true` on every BIN, so passing a seconds-based expiry would be compared against the wrong clock and expire records immediately or never expire them. Use only the hours-based API. (St-H6, fixed: prior versions had a BIN-split bug where right-half records were silently expired — this is now corrected.) | Use only `WriteOptions::with_ttl(hours)` / `with_expiration(hours)`. |
| **`env_db_eviction` (reserved, v3.1)** | Stored but never read; per-database node eviction is not implemented. Setting to `true` emits a `WARN` log. | Has no effect; eviction does not differentiate by database. |
| **Chained / replica-to-replica log feeding (opt-in; ack semantics master-bound)** | Implemented behind `cascade_feeding` (default **off**). When enabled on a mid-tier replica, that replica re-logs the entries it receives from its upstream into its OWN WAL (VLSN-tagged, flushed to the OS — *not* fsync'd: the upstream already holds the durable copy) and serves a downstream replica from its WAL via the same `EnvironmentLogScanner` + `FeederRunner` the master uses (faithful to JE `MasterFeederSource`/`FeederManager`, which run a feeder on any node holding the data). The master also feeds pull-path replicas from its WAL. **Bound:** the master remains the durability/ack authority — a downstream replica's ack is observed by *its* upstream feeder, but durability acknowledgement is **not** propagated transitively up the chain to the master's `AckTracker`; commit-quorum durability is still computed against replicas the master feeds directly. (Replica-to-replica *file-level* copy also exists via network restore.) | Enable `cascade_feeding` on mid-tier replicas to offload the master's outbound fan-out. Do **not** rely on a chained downstream replica to satisfy the master's commit-durability quorum; size the directly-fed electable set for the durability you need. |
| **Database/transaction triggers (implemented — Unreleased)** | The public [`Trigger`](https://docs.rs/noxu-db) trait (`crates/noxu-dbi/src/trigger.rs`, re-exported from `noxu-db`) is a faithful port of JE `com.sleepycat.je.trigger.Trigger` + `TransactionTrigger`. Register on a `DatabaseConfig` via `with_trigger(Arc<dyn Trigger>)` / `add_trigger(...)` (JE `DatabaseConfig.setTriggers`); multiple triggers fire in registration order. `put(txn_id, key, old_data, new_data)` / `delete(txn_id, key, old_data)` fire within the transaction after the change (JE `Trigger.put`/`delete` via `TriggerManager.runPutTriggers`/`runDeleteTriggers`); `commit(txn_id)` / `abort(txn_id)` (default no-op) fire on resolution, once per modified database (JE `runCommitTriggers`/`runAbortTriggers` over `Txn.getTriggerDbs()`). **Adaptation (diverges from JE):** like the DBI-14 comparator API, triggers are **runtime-registered only — not persisted, not replicated** (a Rust closure has no reconstructable class name, unlike JE `PersistentTrigger`); re-register on every open. This matches JE's own "only transient triggers are currently supported" state. | Register triggers via `DatabaseConfig::with_trigger` on every open; do not rely on persistence or replication of triggers. |
| **Admin tooling (dump / load / print-log) (implemented 2026-06)** | A `noxu-admin` CLI (built from `crates/noxu-db/src/bin/noxu_admin.rs`) provides three read-mostly utilities, faithful ports of JE `DbDump` / `DbLoad` / `DbPrintLog`. **`dump`** opens the environment read-only, walks a database cursor, and writes the classic `db_dump` text format (`VERSION=3` header, `format=print` or `format=bytevalue`, `type=btree`, `dupsort=0/1`, `HEADER=END`, then alternating space-prefixed key/data lines, `DATA=END` terminator). Encoding is byte-for-byte JE's `CmdUtil.formatEntry`: printable bytes (ASCII 33..126) emitted literally with backslash doubled, non-printable bytes as `\HH`; or every byte as 2-digit hex in `bytevalue` mode. **`load`** is the inverse (JE `DbLoad`): it parses the header, then puts each key/data pair in one transaction, creating the target database. `dump \| load` round-trips any database **including binary (non-UTF-8) keys/values** — the headline test (`crates/noxu-db/tests/admin_cli_test.rs`) round-trips an all-256-byte-values record, newline/backslash/NUL bytes, and duplicate keys in both `print` and `bytevalue` formats. **`print-log`** walks the WAL via a read-only `FileManager` + `LogFileReader` (no recovery, works on a closed env), printing per-entry `lsn=… type=… size=…` plus decoded txn id and key/data sizes for LN and Txn-end entries; `-S` prints a per-type summary. **Dup-sort caveat**: Noxu does not persist the sorted-duplicates flag across a reopen, so `dump` cannot auto-detect it — pass `-D` to dump a duplicates database (symmetric to JE `DbLoad -c dupsort=true`). JE ref: `util/DbDump.java`, `util/DbLoad.java`, `util/DbPrintLog.java`, `utilint/CmdUtil.java`. | `noxu-admin dump -h <env> -s <db> [-f out] [-p] [-D]`, `noxu-admin load -h <env> -s <db> [-f in] [-n]`, `noxu-admin print-log -h <env> [-S]`. Build with `cargo build -p noxu-db --bin noxu-admin`. See `docs/src/operations/admin-tooling.md`. |
| **Code coverage not tracked in CI** | A `make coverage` target (`cargo-llvm-cov`) exists but coverage is not measured or gated in CI; there is no committed coverage baseline. | Run `make coverage` locally to inspect coverage of changed code. |
| **Spec models are protocol models, not conformance proofs** | The `noxu-spec` Stateright specs model-check the *protocol design*'s safety/liveness; they are abstract models kept in sync with the Rust by review convention (two anchor to production types), not a mechanical refinement proof of the implementation. | A green spec means the protocol is safe; rely on the unit/integration suites for implementation conformance. |
| **Cleaner: `FilesToMigrate` (`forceCleanFiles`) implemented internally (CLN-8 done, internal API)** | The cleaner now has a force-clean set (JE `Cleaner.forceCleanFiles` / `FilesToMigrate`): `Cleaner::set_force_clean_files` / `add_force_clean_file` / `clear_force_clean_files` / `get_force_clean_files`, backed by a `BTreeSet<u32>` on the `FileSelector`. The third selection tier in `select_file_for_cleaning_with_policy` (JE `UtilizationCalculator.getBestFile`'s forceCleaning/`filesToMigrate` tier) prefers a **safe-to-clean** file from the set — age-eligible, not in-progress, not inside the oldest open transaction's log window (the CLN-4 clamp) — over the utilization-selected candidate, and drains it from the set once selected. An unsafe forced file stays in the set and is skipped. Reachable via `EnvironmentImpl::get_cleaner()`. **Not yet exposed on the `noxu-db` public `Environment` / `noxu-admin` surface** (deferred to keep the diff small and low-risk). | Use when an operator needs to compact specific log files. Access via the cleaner (`get_cleaner().set_force_clean_files([..])`); a public admin path is a follow-up. |
| **Cleaner: persisted `FileSummaryLN` entries read back at recovery (CLN-4 / C7, fixed 2026-06)** | The checkpointer's `persist_file_summaries` writes a `FileSummaryLN` log entry per tracked file carrying the **full** `FileSummary` breakdown (per-type LN/IN total + obsolete counts + maxLNSize) **and** the obsolete-offset `PackedOffsets` (C7), and recovery now reads those entries back during the env-open analysis scan and seeds the cleaner's `UtilizationProfile` (`EnvironmentImpl` → `Cleaner::seed_profile`). After a restart the cleaner's profile (`get_profile_summary` / `get_file_summary_map`) shows real utilization **immediately**, so an under-utilized file is selectable for cleaning without re-warming from new writes. Recovery also counts obsolete the LN supersessions written *after* each file's last `FileSummaryLN` (the `abort_lsn` → `isFileUncounted(oldFile, newLsn)` gate), so obsolete bytes produced after the last checkpoint are not lost. JE: `UtilizationProfile.populateCache` + `RecoveryUtilizationTracker.countObsoleteIfUncounted`. **Still open (CLN-11)**: the persisted summaries live only in the WAL (read once at recovery), not in a queryable `FileSummaryDB` updated live; and the per-DB `DbFileSummary` axis is still re-accumulated by live writes (the per-FILE axis is now seeded). | After a crash the cleaner sees per-file obsolete bytes immediately and can reclaim already-obsolete files without waiting for live writes to re-warm the tracker. (Was: cleaner started "blind" and disk footprint stayed large until the tracker re-warmed.) |
| **Cleaner: `UtilizationTracker` detail is budget-bounded (DBI-24, fixed 2026-06)** | The in-memory `UtilizationTracker` accumulates per-file obsolete-offset detail (a `Vec<u32>` of obsolete LSN offsets per tracked file). A cleaner that falls far behind would grow this detail without bound. The tracker now carries a byte budget = `cache_size * CLEANER_DETAIL_MAX_MEMORY_PERCENTAGE / 100` (default 2%), wired at env-open from the cache size and the (now read) config parameter. `UtilizationTracker::evict_memory` drops obsolete-offset DETAIL when over budget — flushing any single tracked file holding ≥ 1 MB of detail immediately, otherwise flushing the largest flushable file once the small-file total exceeds the budget. The dropped data is **only** the per-LSN offset detail; the aggregate `FileSummary` counts that drive util% file-selection are **preserved**, so a detail flush never changes which files the cleaner selects (it only reduces exact-cleaning precision, which JE accepts). Faithful to JE `UtilizationTracker.evictMemory` + `MemoryBudget.getTrackerBudget` (`= cachePortion * CLEANER_DETAIL_MAX_MEMORY_PERCENTAGE / 100`). **noxu adaptation**: JE's `flushFileSummary` persists a `FileSummaryLN` then calls `TrackedFileSummary.reset` (which zeroes the in-memory summary because it was just persisted); noxu's tracker cannot reach the `UtilizationProfile` from this layer, so it drops detail while keeping aggregates in memory. | None needed — tracker admin-memory is now bounded. A cleaner backlog can no longer grow the tracker's offset detail past `CLEANER_DETAIL_MAX_MEMORY_PERCENTAGE` of cache; exact-cleaning precision degrades gracefully (a re-lookup at clean time) rather than consuming unbounded memory. |
| **Cleaner: `ExpirationProfileStore` persists across restart (CLN-24/CLN-26 done)** | The per-file expiration histogram is maintained in memory AND now persisted: at each checkpoint the serialized per-file expiration histogram is written into the `FileSummaryLN` entry (alongside the C7 utilization breakdown), and recovery restores it into the seeded `FileSummary`'s `obsolete_expired_size` / `obsolete_expired_gradual_size` band via the CLN-4 inline rebuild seam — so the cleaner's TTL-adjusted file selection (the two-pass gate) sees the expiration prediction immediately after restart instead of recomputing from scratch. **CLN-26**: the gradual-expiration band now prorates over the whole **day** for day-aligned (day-TTL) data, not just the hour, matching JE `ExpirationProfile.getExpiredBytes`. JE: `ExpirationTracker.serialize` / `ExpirationProfile`. | None — expiration prediction survives restart. (The serialized histogram is appended to `FileSummaryLN` length-prefixed; older entries without it read as an empty histogram.) |
| **Cleaner: `wakeupAfterNoWrites` wired to Checkpointer daemon (CLN-14 done)** | `Cleaner::with_checkpoint_wakeup_fn` is invoked after each successful cleaning pass, and `noxu-dbi` now registers it (via `Cleaner::set_checkpoint_wakeup_fn`) to call `Checkpointer::wakeup_after_no_writes`, which wakes the checkpointer daemon's sleep condvar so it re-evaluates `is_runnable` (true via `needs_checkpoint_for_cleaned_files`) and checkpoints early. Cleaned files are now deleted promptly when write activity stops, instead of waiting the full checkpointer wakeup interval (default 60 s). JE: `FileProcessor.doClean` → `envImpl.getCheckpointer().wakeupAfterNoWrites()`. | None — cleaned files are reclaimed promptly on an idle environment. |
| **Compressor: BIN slot removal consults the lock manager (IC-3, defended 2026-06)** | `Tree::compress_bin` now delegates to `compress_bin_with_lock_check(bin, is_locked: Option<&dyn Fn(u64)->bool>)`, which SKIPS any `known_deleted` slot the caller-supplied predicate reports as still write-locked by an in-flight transaction — a faithful port of JE `BIN.compress` (`BIN.java:1141-1172`), which calls `lockManager.isLockUncontended(lsn)` and does `anyLocked = true; continue;` on a contended slot. The dbi layer (`environment_impl.rs`: the INCompressor daemon and `compress_all`) supplies `move \|lsn\| lock_manager.get_lock_info(lsn) != (0, 0)` — the inverse of JE's `isLockUncontended` (`nWaiters == 0 && nOwners == 0`). `noxu-tree` gains **no** `noxu-txn` dependency: the predicate is a `dyn Fn`, the lock knowledge lives in the closure (the layering boundary holds). A `NULL_LSN` slot is discarded without consulting the predicate (JE: "Can discard a NULL_LSN entry without locking"). When no predicate is supplied (recovery / BIN-delta replay / lock-manager-less tests) behavior is unchanged — all `known_deleted` slots are removed. **Lock ordering (no deadlock):** the predicate runs while `compress_bin` holds the BIN write latch; `get_lock_info` takes a lock-table shard mutex for one short, non-blocking critical section and releases it before returning, and the LockManager never latches a BIN (lock acquisition descends from the dbi/cursor layer, never the reverse), so the only edge is BIN-latch -> shard-mutex (acyclic). **Why this matters:** it removes the previously fragile implicit invariant ("no write path ever leaves an uncommitted, write-locked tombstone in a `BinStub`"). Even if a future write path violates that, the compressor will no longer physically remove a slot a live txn still references. | Defended. No reliance on the implicit invariant remains. |
| **Some cleaner/evictor tuning config parameters are not yet wired** | A few JE config parameters control features whose underlying model is not fully ported, so the parameters are accepted but ignored: `BIN_DELTA_BLIND_OPS` / `BIN_DELTA_BLIND_PUTS` (blind-put-on-BIN-delta infra exists but production never enables it); `EVICTOR_MUTATE_BINS`, `EVICTOR_FORCED_YIELD`, `CLEANER_RMW_FIX`, `CLEANER_GRADUAL_EXPIRATION` (features not ported); `RESERVED_DISK` (disk-full reservation not implemented). | None needed for correctness — these are tuning knobs. `CLEANER_TWO_PASS_GAP`/`CLEANER_TWO_PASS_THRESHOLD`, `EVICTOR_USE_DIRTY_LRU`, `EVICTOR_EVICT_BYTES`/`EVICTOR_CRITICAL_PERCENTAGE`, `EVICTOR_ALGORITHM` (cache eviction policy, default LRU), and `LOCK_N_LOCK_TABLES` were previously in this set and are now wired. |
| **Eviction now reclaims to budget across all DB trees (EVICTOR-RECLAIM-1, fixed 2026-06)** | Under a working set larger than the cache the evictor previously *targeted* nodes but reclaimed almost none: with a 16 MiB cache over a ~21 MB working set, resident usage stayed ~24 MB (~1.45× over budget), `stripped~1`, `freed~0`. Two defects combined and are now fixed: (1) **split-created BINs/INs were never registered with the evictor LRU** — only the first-key root+BIN and re-fetched nodes called `note_added`, so after the tree grew past its first BIN every later BIN was invisible and the policy lists were nearly empty; the `InListListener` is now threaded through `insert_recursive`/`split_child` so a freshly-split node is registered the instant it becomes resident (JE `IN.splitInternal` → `inList.add(newSibling)`); and (2) **the evictor searched only a single primary tree slot**, so a second database's BINs were targeted but never found/stripped; the evictor now consults the shared `db_trees_registry` (the same registry the checkpointer/cleaner use) to find each candidate's owning tree and operates on it (JE `Evictor.processTarget` → `target.getDatabase()`). Measured after the fix (16 MiB cache, ~21 MB across two user DBs, real disk): `stripped 790`, `freed ~16 MB`, resident ~0.53× budget, all records re-fetch correctly. Regression guard: `crates/noxu-db/tests/evictor_reclaim_multitree_test.rs`. The eviction *policy* (`EVICTOR_ALGORITHM`) now genuinely affects the resident set; **default stays LRU** (JE-faithful). | None needed — eviction now bounds resident memory to ~the configured `cache_size` under pressure. Data was always correct (evicted/stripped LNs re-read from the log); now resident memory is also bounded. |
| **Disk-limit enforcement (`MAX_DISK` / `FREE_DISK`) (implemented 2026-06)** | The `noxu.maxDisk` / `noxu.freeDisk` config parameters are now **enforced** on the user-write path. A `DiskLimitTracker` (faithful port of JE `Cleaner.recalcLogSizeStats`) caches a volatile violation flag computed from total log size + filesystem free space (`fs2::available_space` / statvfs) against `MAX_DISK` (absolute log-size cap) and `FREE_DISK` (keep-this-much-free reserve). `CursorImpl::put`/`delete` consult the cached flag (a single atomic load — no per-write statvfs) and return `NoxuError::DiskLimitExceeded` BEFORE logging or mutating the tree once a limit is violated, matching JE `Cursor.checkUpdatesAllowed` → `DiskLimitException`. **Internal databases are exempt** (JE `dbImpl.getDbType().isInternal()`): the cleaner/checkpointer/recovery writes — which FREE space — are never blocked, so the env never deadlocks at the limit. The flag is refreshed periodically by the checkpointer daemon and after every cleaner pass (JE `Cleaner.manageDiskUsage` → `freshenLogSizeStats`), and at env-open during recovery; once space is reclaimed the next refresh clears the violation and writes resume. **Default behaviour unchanged**: `MAX_DISK` defaults to 0 (cap disabled); `FREE_DISK` defaults to 5 GiB and only trips below 5 GiB free (JE default). When both are 0 the tracker is inert (the check is a single branch, no statvfs). Headline test: `crates/noxu-db/tests/disk_limit_test.rs`. JE ref: `cleaner/Cleaner.java` `recalcLogSizeStats`/`getDiskLimitViolation`, `dbi/EnvironmentImpl.java` `checkDiskLimitViolation`, `Cursor.java` `checkUpdatesAllowed`. | Set `EnvironmentConfig::with_max_disk(bytes)` for an absolute log-size cap and/or `with_free_disk(bytes)` for a free-space reserve; writes are refused with `DiskLimitExceeded` while a limit is violated and resume automatically once the cleaner reclaims space. External free-space monitoring is still prudent, but the engine now refuses new user writes before the disk fills so recovery stays possible. |
| **Shared cache across environments (`SHARED_CACHE`) (implemented — Unreleased)** | The `noxu.sharedCache` parameter is now **wired**: multiple `Environment`s opened with `EnvironmentConfig::with_shared_cache(true)` join a **process-global shared evictor** (a faithful port of JE `SharedEvictor` + the shared `MemoryBudget`).  All sharing envs share ONE `Arc<Evictor>`, ONE memory budget (sized from the FIRST joining env's `cache_size` — JE-faithful), and ONE global LRU spanning every registered env's B-trees; eviction picks victims across ALL sharing envs' trees, not per-env, so total resident stays bounded by the ONE shared budget instead of the sum of per-env budgets.  On `Environment::close` the env DEREGISTERS its trees from the shared LRU before they drop (no dangling trees / use-after-close), and the shared evictor tears down when the last member leaves.  `shared_cache=false` (the default) is entirely unchanged: a private per-env evictor + budget + daemon, exactly as before.  JE ref: `evictor/SharedEvictor.java`, `dbi/MemoryBudget.java` (shared), `EnvironmentConfig.setSharedCache`.  Headline test: `crates/noxu-db/tests/shared_cache_test.rs`; DST shuttle coverage of the register/deregister/scan interleavings: `crates/noxu-evictor/tests/shuttle_shared_cache.rs`. | Set `EnvironmentConfig::with_shared_cache(true)` on every `Environment` that should share ONE cache budget in a process; the first env's `cache_size` sizes the shared budget.  Leave it `false` (default) for an independent per-env cache. |
| **IN cached-node heap-footprint compactions: `INTargetRep` done; `INKeyRep`/`INLongRep` deferred (T-2/T-3/T-4/T-5)** | The cached-IN/BIN heap layout is compacted vs JE on the **target-pointer** axis but not yet on the key/LSN axes. **T-4 (done):** the resident-child pointer is a node-level `INTargetRep` (`None`/`Sparse`/`Default`) — an upper IN with no resident children costs 0 child-pointer bytes instead of one `Option<Arc>` per slot (`tree::TargetRep`, faithful to `INTargetRep.java`). **T-3 (deferred):** per-slot LSNs are still a full 8-byte `Lsn` per entry rather than JE's `INLongRep` min-byte-width packing. A faithful port is blocked on `NULL_LSN == u64::MAX`: JE's `INLongRep` requires non-negative values and would force 8-byte width for any node holding a single NULL slot (the common case), defeating the win — JE handles this with a `baseFileNumber`-relative `entryLsnByteArray` + a reserved transient-offset encoding, which is the actual scope to port. **T-2/T-5 (deferred):** per-slot keys are still a `Vec<u8>` (24-byte header + heap alloc each) rather than JE's `INKeyRep.MaxKeySize` single fixed-width `byte[]` for all-small-key INs, so the `TREE_COMPACT_MAX_KEY_LENGTH` config param (default 16) remains accepted-but-inert (T-5). T-2 is the most invasive (touches every key access — find_entry / prefix / split / serialize). | None needed — these are heap-footprint optimizations, not correctness. The memory budget (DBI-23) accurately accounts for the current layout, so cache sizing is correct; the deferred axes only reduce how much each cached node costs. Implement T-3 by porting JE's `baseFileNumber` LSN packing; T-2/T-5 by porting `INKeyRep.MaxKeySize` and threading `TREE_COMPACT_MAX_KEY_LENGTH` to the tree. |
| **Diagnostic & minor feature gaps vs JE (low-severity, catalogued)** | A set of JE diagnostic/perf features are not implemented; none affects correctness: no per-latch contention stats or owner-stack capture (L-1/L-2), no lazy trace-log queue (L-7), no multi-data-directory log spreading (`je.log.nDataDirectories`, L-11), no FileCacheWarmer startup pre-read (L-12), no FileDeletionDetector WatchService monitor (L-13), no `CacheMode::DYNAMIC` env-default mode (EV-4), no `CountEstimator` sampling count (Noxu uses exact counts — stronger, T-16), and several evictor LRU second-chance/skip nuances (EV-5/8/9/12/18) plus off-heap stat breadth (EV-31, off-heap is inert by default). The `EVICTOR_MUTATE_BINS` and `TREE_COMPACT_MAX_KEY_LENGTH` config params are accepted but inert (EV-11/T-5, tied to unported features — T-5 tracked under the heap-footprint-compaction row above). **L-3 (implemented, 7.1):** the debug-build latch-ordering assertion is now present in `noxu-latch` (`latch_order` module + `LatchContext::rank`); like JE's it is compiled out in release builds (`#[cfg(debug_assertions)]`) and is a developer/test invariant with zero release-build cost. | None needed — these are observability/perf/tuning gaps, not correctness. Use external monitoring for latch/cache contention; rely on the engine's own stats (`get_stats`) for the metrics that are implemented. The L-3 latch-ordering check catches lock-ordering bugs in dev/test when a subsystem assigns strictly-increasing latch ranks. |
| **Accepted-but-inert `EnvironmentConfig` knobs (DBI-14 sweep)** | A set of config fields are stored but never read on a runtime path. **Deprecated-moot (7.1):** the JE-style logging/tracing knobs (`logging_level`, `console_logging_level`, `file_logging_level`, `trace_console`/`trace_db`/`trace_file`/`trace_level`/`trace_file_count`/`trace_file_limit_bytes` and the per-subsystem `trace_level_*`), `adler32_chunk_size`, and `env_dup_convert_preload_all` are now `#[deprecated]` (removed in 8.0) and are NO LONGER in the `unimplemented_params` WARN registry — they configure features Noxu deliberately does not have (logging routes through the `log` crate / `noxu-observe` / `RUST_LOG`; Noxu uses CRC32 not Adler32; the `.ndb` format has no legacy dup format to convert). The functional-sounding orphans `checkpointer_min_interval_secs` (not a JE param — the checkpointer is driven by `checkpointer_bytes_interval` + `checkpointer_wakeup_interval_ms`, both wired), `verify_schedule` (no background btree verifier scheduler), and `dos_producer_queue_timeout_ms` (DiskOrderedScan producer-queue timeout not consulted) remain inert and warned via `unimplemented_params.rs`. (`exception_listener`, `stats_file_*`, `startup_dump_threshold_ms`, and `env_check_leaks` are now IMPLEMENTED — see the diagnostics rows below.) Setting a non-default inert knob logs a `warn!` at `Environment::open`. | None needed — these are observability/diagnostics/tuning knobs, not correctness. Configure logging via the `log` facade / `RUST_LOG`; read live stats via `get_stats`; tune checkpoints via the wired `checkpointer_bytes_interval` / `checkpointer_wakeup_interval_ms`. |
| **Custom comparators require an application-registered identity (DBI-14/15)** | User B-tree and duplicate comparators are supported: `DatabaseConfig::with_btree_comparator` / `with_duplicate_comparator` take a `Comparator` (an `Arc<dyn Fn(&[u8],&[u8]) -> Ordering>` paired with a stable **identity** string), and the `override_btree_comparator` / `override_duplicate_comparator` flags are now meaningful (JE `setOverrideBtreeComparator`). The one adaptation vs JE: JE persists the comparator's **class name** and reconstructs the `Comparator` instance by class name at open (`DatabaseImpl.ComparatorReader`); a Rust `Fn` has no portable name, so Noxu persists the application-supplied **identity** and the application must re-supply a comparator with the matching identity on every open (and on every replica). A mismatch fails the open with `ComparatorMismatch` rather than silently misorder — JE's mismatch semantics. JE ref: `DatabaseConfig.setBtreeComparator` / `ComparatorReader`. See design-decisions §12. | None — set the comparator (with a stable identity) on every `DatabaseConfig` used to open the database, including on replicas. Without it, an existing comparator-ordered database refuses to open (it will not silently fall back to byte order). |
| **Replica live reads served with enforced read-consistency policies (REP-7 + REP-10 done)** | A streaming replica APPLIES the master's committed log entries to a LIVE in-memory B-tree as they stream in (`noxu_dbi::ReplicaReplay`, wired through `EnvironmentLogWriter::with_replay`), reusing the same `noxu_recovery::apply_redo_ln` the crash-recovery redo pass uses. A read on the replica reads the live tree and returns the master's committed data WITHOUT a restart. Uncommitted master txns are applied provisionally and become visible only when their commit streams in (JE `Replay.replayEntry`). The replica still writes the WAL (durability); on a replica crash, recovery re-derives the SAME tree the live-apply produced (verified by `rep7_crash_consistency_test`). **REP-10 (done):** `ReplicaConsistencyPolicy` (NoConsistency / TimeConsistency / CommitPointConsistency) is now ENFORCED on the replica read path. A read on a replica with a non-`NoConsistency` policy BLOCKS in `ReplicatedEnvironment::begin_read_consistency` until the replica's `last_applied_vlsn` (the REP-7 hook) satisfies the policy, or the policy timeout expires (a clean `RepError::ConsistencyTimeout` / `ReplicaLagExceeded`, never a hang). A master commit mints a `CommitToken` (`ReplicatedEnvironment::commit_token`); a client passes it to a replica read via `ConsistencyPolicy::commit_point(&token, timeout)`, and the read blocks until the replica has replayed past that VLSN. Port of JE `Replica.ConsistencyTracker.awaitVLSN` / `lagAwait` + `CommitToken`. The default policy is `NoConsistency` (unchanged behaviour unless a policy is set). | For read-after-write across a load-balanced replica set, capture the master's `CommitToken` on the write and pass it to the replica read via `CommitPointConsistency`; for a bounded staleness window use `TimeConsistency`. `NoConsistency` (the default) reads may still lag the master — route to the master or set a policy if that matters. |
| **Recovery verification: LSN↔utilization-profile overlap check (fixed 2026-07)** | **Resolved.** `Environment::verify` now runs BOTH halves of JE's recovery verification: the live-tree structural walk (child accessibility, key-range containment, non-deleted-slot LSN validity) AND the `VerifyUtils.checkLsns()` overlap check — asserting the set of live tree LSNs is DISJOINT from the obsolete LSNs recorded in the `UtilizationTracker`. The engine tree walk gathers the live LN LSNs (`noxu_engine::gather_tree_lsns`, JE `GatherLSNs`), the cleaner supplies the obsolete set at per-LSN offset granularity from each `TrackedFileSummary`'s obsolete-offset detail (`noxu_cleaner::check_lsns`, JE `UtilizationProfile.getObsoleteDetailPacked` + `DbLsn.makeLsn`), and any live LSN found in the obsolete set is reported as a `DataInconsistency` verify error (JE "Obsolete LSN set contains valid LSN" → `LOG_INTEGRITY`). The `UtilizationTracker` is threaded into the verifier through `EnvironmentImpl::get_utilization_tracker`. Every recovery test (`recovery_correctness_test`, `crash_recovery_test`) now asserts LSN↔profile disjointness after recovery, so a recovery that produces a correct tree but a utilization profile mislabelling a live LSN as obsolete FAILS verification. | None needed. |
| **DPL secondary indexes are transactional and persistent (fixed 2026-06)** | **Resolved.** In the `noxu-persist` DPL layer, secondary indexes are now real, persistent `noxu-db` `SecondaryDatabase`s opened against the entity's primary database via `EntityStore::open_secondary_index` (the JE `Store.openSecondaryDatabase` model). They are maintained **inside the active transaction** by the primary `put` / `delete` automatic-maintenance fan-out: aborting a transaction rolls the primary write **and** the secondary index update back together, and a committed transaction makes both visible. The secondary survives store reopen (it is persistent, not rebuilt from a side map). The former one-shot `log::warn!` and `PersistError::SecondariesNotTransactional` have been removed. | None needed. The DPL secondary path is now atomic with the user transaction. (Open a secondary with `EntityStore::open_secondary_index(&mut primary, name, serializer, extractor)`, or via the `#[derive(SecondaryKey)]`-generated `open_<name>_index(&mut store, &mut primary, serializer)` helper.) |
| **Collections iterators are snapshots, not live cursors** | `noxu-collections` `StoredIterator` captures a snapshot of keys at `iter()` time (participating in the caller's transaction), whereas JE's `StoredIterator` holds a live cursor that re-reads as it advances. This is a deliberate design choice (avoids holding a cursor open across the iteration) but means modifications made after `iter()` is called are not observed by an in-flight iteration. | Re-create the iterator to observe concurrent modifications; or use a `noxu-db` `Cursor` directly for live-cursor semantics. |
| **DPL composite primary-key on-disk format changed in v4.x (PERSIST-COMP-1)** | `#[derive(PrimaryKey)]` for a multi-field key struct previously length-prefixed each field (`[4-byte BE len][bytes]`), which sorted keys by field lengths instead of logical tuple order — corrupting ordered iteration / range scans. The encoding is now order-preserving and self-delimiting with no length prefix (fixed-width numerics by width; `String`/`Vec<u8>` as a `0x00`-terminated escaped byte string, mirroring JE `TupleOutput.writeString`). This is a **breaking on-disk change**. | Rebuild (dump + reload) any DPL store whose entities use a multi-field `#[derive(PrimaryKey)]`. Single-field newtype keys are unaffected. No production users on v4.x, so no in-place converter is provided. |
| **Tuple string encoding is not wire-compatible with JE** | `noxu-bind` tuple string encoding uses a double-terminator + escaped UTF-8, whereas JE uses a single `0x00` terminator + modified-UTF-8. Noxu's encoding round-trips correctly within Noxu but is NOT byte-compatible with JE-encoded tuples. (Noxu uses a Rust-native on-disk format and does not target JE interop — see the `.ndb` format decision.) | None needed for Noxu-only use. Do not attempt to read JE-encoded tuple data with Noxu or vice versa. |
| **Replication HA protocol is incomplete (election ranking, syncup, master transfer)** | Some JE replication protocol components are still partial. **Implemented since this row was written**: the DTVLSN substrate and tracking (`get`/`set`/`update_dtvlsn`, master-side `update_dtvlsn_from_feeders`), DTVLSN-based election ranking (`Proposal.dtvlsn` is the major ranking key, wired into production `run_election_with_phi_dtvlsn`), and `is_authoritative_master` partition/quorum detection. **Still partial**: the `CommitFreezeLatch` exists as a primitive but is not yet wired into the replica replay / acceptor paths (VLSN can still advance mid-election); the bilateral syncup matchpoint protocol does not roll a *diverged* replica back to a verified matchpoint (`negotiate_syncup` is a range-availability check only — risk of divergent/lost writes on a diverged tail); and there is no periodic `DTVLSNFlusher` daemon. (`transfer_master` itself does change master — see its own row.) | **Do not rely on automatic failover / master election for correctness in production.** Replication is suitable for read-scaling and warm-standby with operator-supervised failover only. See the replication chapter; full HA election/syncup fidelity is in progress. |

---

## Deliberately not implemented (diagnostics / perf), with reasons

The following JE diagnostic, observability, and micro-optimization features are
**intentionally not implemented**.  None affects correctness; each has a clear
reason it is not worth porting to Noxu.  This is honest engineering: we say
what won't be done and why.

- **L-1 / L-2 — per-latch contention stats + owner-stack capture:** external
  tools (perf, flamegraphs, tokio-console-style profilers) give better
  latch-contention insight than in-process counters, and Noxu already exposes
  aggregate lock stats via `get_stats()`; owner-stack capture is a JVM-only
  debug convenience.
- **L-7 — lazy trace-log queue:** the `log` / `tracing` crates already provide
  async / non-blocking logging, so a bespoke in-engine trace queue is redundant.
- **L-11 — multi-data-directory log spreading (`je.log.nDataDirectories`):**
  use RAID / LVM / ZFS striping at the storage layer — the OS / filesystem does
  this better than the DB, and the complexity-vs-payoff is poor.
- **L-12 — FileCacheWarmer startup pre-read:** the kernel page cache warms
  implicitly on access, so an explicit startup pre-read is a marginal
  micro-optimization.
- **L-13 — FileDeletionDetector WatchService:** JE's is built on the
  Java-specific `java.nio` `WatchService`; it is not portable, and OS-level file
  monitoring (inotify / FSEvents) can be run externally if needed.
- **EV-4 — `CacheMode::DYNAMIC` environment-default mode:** a JE eviction
  micro-optimization with diminishing returns; Noxu's LRU default is faithful
  and correct.
- **EV-5 / EV-8 / EV-9 / EV-12 / EV-18 — LRU second-chance / skip nuances:**
  these are JE evictor micro-optimizations with diminishing returns; Noxu's LRU
  is faithful and correct without them.
- **EV-31 — off-heap stat breadth:** off-heap caching is opt-in and inert by
  default, so the extra off-heap stats are purely informational.
- **T-16 — `CountEstimator` sampling count:** Noxu returns EXACT record counts,
  which is stronger than JE's sampled estimate — implementing sampling would be
  a regression, so it is deliberately not done.

(Implemented in 7.1 and moved out of this list: **L-3** debug-build
latch-ordering assertion, **`exception_listener`**, **stats-file dump**
`STATS_FILE_*`, **`startup_dump_threshold_ms`**, and **`env_check_leaks`** —
see the rows in the table above.)

---

## Quick-reference: `EnvironmentConfig` production defaults

```rust
EnvironmentConfig::new(path)
    .with_allow_create(true)
    .with_transactional(true)
    // Cache: 30% of available RAM, e.g. 8 GiB on a 32 GiB machine
    .with_cache_size(8 * 1024 * 1024 * 1024)
    // Log files: 64 MiB each (larger = less cleaner overhead)
    .with_log_file_max_bytes(64 * 1024 * 1024)
    // Checkpoint every 128 MiB written
    .with_checkpointer_bytes_interval(128 * 1024 * 1024)
    // Start cleaning files that are < 60% live (default 50%)
    .with_cleaner_min_utilization(60)
    // Group commit: batch up to 32 writers, flush every 2 ms
    .with_log_group_commit(32, 2)
    // Lock / txn timeouts to detect deadlocks quickly
    // (set via EnvironmentMutableConfig after open)
```
