CLAUDE.md
fuzz-*.log
*.ndb
timing_test*
default.db
crash_test_files
*conf
*snap.*
*grind.out*
vgcore*
# Process core dumps (Linux/macOS). Never commit these — they are very
# large and may contain sensitive in-memory state. Anchored to avoid
# accidentally ignoring a future module or directory called "core".
/core
**/core.[0-9]*
*.bk
*orig
tags
perf*
*folded
*out
*perf
*svg
experiments
*.rlib
target
Cargo.lock
*swp
*swo
*.proptest-regressions
corpus
artifacts
.idea
cargo-timing*
.memelord
.agent
# Agent skill files are tracked even though everything else under
# .agent/ is private — they are referenced from AGENTS.md and need
# to be visible to every contributor.
!.agent/
!.agent/skills/
!.agent/skills/*.md
_
.jdb
# TLA+ TLC scratch directories and logs
tla/states/
